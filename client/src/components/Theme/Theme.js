
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Segoe UI font family, sans-serif', // Cambia 'Roboto' por el tipo de letra que desees
  },
  palette: {
    text: {
      primary: '#0B6350', // Cambia '#333' al color de texto que desees
    },
  },
});

export default theme;
